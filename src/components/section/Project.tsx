'use client';

import SectionHeading from '../SectionHeading';

const Project = () => {
  return (
    <div
      id="project"
      className="mt-28 flex max-w-3xl scroll-mt-28 flex-col items-center">
      <SectionHeading>Project & Playground</SectionHeading>
      <div className="h-[20rem] w-full"></div>
    </div>
  );
};

export default Project;
